import ContactSection from "../organisms/ContactSection"

const ContactTemplate = () => (
    <main className="relative z-10 pt-32 pb-section-gap max-w-container-max mx-auto px-margin-mobile md:px-margin-desktop">
        <ContactSection/>
    </main>
)

export default ContactTemplate